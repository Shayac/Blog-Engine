@using Blog.Entities
@model Comment


    @if (Model.ReplyId == null)
    {
        <div class="col-md-3">
            <p>@Model.CommentBody</p>
        </div>
    }
    else
    {
        <div class="col-md-3 col-md-offset-1">
            <p>@Model.CommentBody</p>
        </div>
    }
